import React from "react";
import styles from "./Widget.module.css";

const BlobContainer = () => (
  <div className={styles.blobContainer}>
    {/* 3D Blob canvas or SVG here (placeholder) */}
  </div>
);

export default BlobContainer;